language: "haskell"
install:
      - "cabal update --verbose=2"
      - "cabal install --only-dependencies"
      - "cabal configure --enable-tests"
      - "cabal build"
      - "cabal test"
      - "cabal install"
notifications:
    email: true
